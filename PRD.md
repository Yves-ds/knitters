# 니터즈 (Knitters) — 제품 요구사항 문서 (PRD)

> **버전** 1.1 · **최종 업데이트** 2026-05-28  
> **플랫폼** 모바일 웹 (Single-Page Application)  
> **배포 URL** https://yves-ds.github.io/knitters/

---

## 1. 제품 개요

### 1.1 제품 정의
니터즈(Knitters)는 뜨개질을 즐기는 사람들을 위한 올인원 커뮤니티 & 프로젝트 관리 앱이다. 사용자는 진행 중인 뜨개 작품을 기록·관리하고, 커뮤니티에서 다른 니터들과 소통하며, 함께 뜨기 모임(함뜨해요)에 참여할 수 있다.

### 1.2 핵심 가치
| 가치 | 설명 |
|------|------|
| **기록** | 작품별 진행 상황, 타이머, 단수 카운터, 사진·도안·영상을 한 곳에 |
| **커뮤니티** | 뜨개 질문·잡담·마켓·구인 게시판과 실시간 검색 |
| **함뜨해요** | 온/오프라인 함께 뜨기 모임 탐색 및 참여 |
| **발견** | 매거진 콘텐츠, 챌린지, 트렌딩 검색어 |

### 1.3 브랜드 컬러
- **Primary** `#F72E00` (로고 레드-오렌지)
- **Gray** `#9CA3AF`, `#6B7280`, `#374151`, `#111827`
- **White** `#FDFCF8`

---

## 2. 사용자

### 2.1 타깃 유저
- 취미로 대바늘·코바늘 뜨개를 즐기는 한국어 사용자
- 뜨개 입문자부터 베테랑 니터까지

### 2.2 계정 유형
| 유형 | 권한 |
|------|------|
| 일반 사용자 | 모든 기본 기능 |
| 관리자 | 실 데이터 관리 (설정 화면 내 숨겨진 메뉴) |

---

## 3. 정보 구조 (IA)

```
앱
├── 인증 플로우
│   ├── 스플래시
│   ├── 로그인 (카카오 / 애플 / 이메일)
│   ├── 이메일 로그인
│   ├── 회원가입
│   └── 계정 찾기 (아이디 / 비밀번호)
│
└── 메인 앱 (바텀 탭 5개)
    ├── 홈
    │   ├── 알림 목록
    │   │   └── 알림 설정
    │   ├── 채팅 목록
    │   │   └── 채팅 방
    │   ├── 매거진 목록 → 아티클
    │   └── 뜨개 가이드
    ├── 기록 (프로젝트)
    │   ├── 프로젝트 상세
    │   └── 새 프로젝트 (기록하기)
    ├── 커뮤니티
    │   ├── 게시글 상세
    │   │   └── 공지사항 (고정 공지 클릭)
    │   ├── 글쓰기
    │   └── 검색
    ├── 탐색
    └── 마이
        ├── 프로필 편집
        └── 설정
```

---

## 4. 화면 정의

### 4.1 인증 플로우

#### 스플래시 (`#splash`)
- 앱 로딩 시 첫 화면
- 브랜드 로고 + "니터들을 위한 뜨개 커뮤니티" 태그라인
- 애니메이션 도트(···) 표시 후 로그인 화면으로 자동 전환

#### 로그인 (`#login`)
- **카카오로 시작하기** 버튼 (노란 배경)
- **Apple로 시작하기** 버튼 (검정 배경)
- **이메일로 시작하기** 버튼 (흰색 테두리)
- 아이디/비밀번호 찾기 링크
- 고객센터 링크

#### 이메일 로그인 (`#email-login`)
- 이메일 입력 (도메인 자동완성: gmail.com, naver.com, kakao.com, daum.net, hanmail.net)
- 비밀번호 입력 (눈 아이콘으로 표시/숨김 토글)
- 잘못된 자격 증명 시 에러 메시지
- 비밀번호 찾기 링크
- 회원가입 유도 링크

#### 회원가입 (`#signup`)
필드별 실시간 유효성 검사:

| 필드 | 제약 | 검사 |
|------|------|------|
| 닉네임 | 최대 20자, 한글·영문만 | 중복 여부 |
| 이메일 | 이메일 형식, 도메인 자동완성 | 중복 여부 |
| 비밀번호 | 8자 이상, 영문 대·소문자 + 숫자 | 강도 표시 |

- 모든 필드 유효 시 "가입하기" 버튼 활성화

#### 계정 찾기 (`#find-account`)
- **아이디 찾기** 탭: 이메일 입력 → 계정 존재 여부 안내
- **비밀번호 찾기** 탭: 이메일 입력 → 재설정 안내

---

### 4.2 홈 (`#home`)

**헤더**
- 채팅 아이콘 (좌)
- 알림 아이콘 (우, 미읽음 시 빨간 점)

**현재 작품 카드**
- 진행 중인 프로젝트 1개 표시
- 상태 배지 (색상 코드는 §6.1 참고)
- 경과 타이머 (00:00:00)
- 프로젝트 썸네일 이모지
- 빈 상태: "진행 중인 프로젝트가 없어요" + 새 프로젝트 버튼

**연속 뜨개 스트릭**
- "N일 연속 뜨개 중" 카운터
- 요일별 스탬프 (월~일)

**니터즈 매거진**
- 가로 스크롤 카드 3개
- 카테고리 태그 + 제목 + 이모지 썸네일
- "전체보기" → 매거진 목록

**뜨개 챌린지**
- 진행 중 챌린지 카드 2개
- 상태 배지, 종료일, 참여자 수
- "더보기" → 챌린지 화면

---

### 4.3 기록 (프로젝트 목록, `#projects`)

**헤더**
- "오늘은 어떤 작품을 떠볼까요?"
- 검색 아이콘

**검색 패널** (활성화 시)
- 검색어 입력 → 프로젝트 이름 필터링
- 취소 버튼 (`#F72E00`)

**정렬 & 필터**
- 정렬: 생성일 / 제목 / 시작일 / 종료일
- 상태 필터: 전체 / 시작 안 함 / 진행 중 / 쉬는 중 / 완성

**프로젝트 카드**
- 상태 컬러 점 + 프로젝트 이름
- 날짜 범위 (시작일 ~ 종료일)
- 썸네일 이미지

**총 작품 수 표시**
- `NN` (굵게) + `개의 작품을 뜨고 있어요` (얇게, font-weight: 400)

**FAB** — "기록하기" 버튼 → 새 프로젝트 화면

---

### 4.4 새 프로젝트 / 프로젝트 편집 (`#record`)

| 요소 | 설명 |
|------|------|
| 상태 배지 | 탭 → 상태 선택 시트 (시작 안 함 / 진행 중 / 쉬는 중 / 완성) |
| 날짜 배지 | 탭 → 달력 시트 (시작일 / 종료일 각각 선택) |
| 프로젝트명 | 텍스트 입력 |
| 리치 에디터 | 텍스트 + 사진 자유 기록 (contenteditable) |
| 사진 추가 | 다중 선택, 순서 드래그 재정렬 (포인터 이벤트 기반), 빨간 드롭라인 표시 |
| 도안 시트 | PDF 등 도안 첨부 |
| 영상 시트 | 영상 참고 링크 첨부 |
| 공유 토글 | ON → 마이 페이지에 공개, 완료 시 토스트 알림 |
| 단수 카운터 | 카운터 1·2 (레이블 편집, +/- 조작, 리셋) |
| 타이머 | 기록 시간 측정 (시작 / 일시정지 / 초기화) |

---

### 4.5 프로젝트 상세 (`#project-detail`)

- 뒤로가기 + 프로젝트명 헤더 + 케밥 메뉴 (수정 / 삭제)
- 상태·날짜 배지 (편집 가능)
- 실행 중 타이머 배지
- 기록 내용 (사진, 텍스트, 도안, 영상)
- 단수 카운터 2개
- 하단 툴바 (사진·도안·영상 추가, 타이머 제어, 공유 토글)

---

### 4.6 커뮤니티 (`#community`)

#### 탭 1: 커뮤니티

**고정 공지** (4개)
- 커뮤니티 이용 규칙
- 5월 챌린지 공지
- 마켓 주의사항
- 이벤트 당첨자 발표

**카테고리 탭** (가로 스크롤)
- 인기 / 뜨개 질문 / 뜨개 잡담 / 자투리 마켓 / 구인·외주

**정렬** — 인기순 / 최신순 토글

**게시글 카드 레이아웃**
1. 카테고리 배지
2. 작성자 (아바타 27px + 닉네임 + 팔로우 버튼)
3. 제목 (15px SemiBold)
4. 본문 미리보기 (2줄, overflow hidden)
5. 하단: 날짜·조회수 (좌) + 좋아요·댓글 수 (우)

카드 스타일: 흰 배경, border-radius 14px, 그림자 (`0 4px 16px rgba(0,0,0,0.10)`)

**FAB** — "글쓰기" 버튼 → 게시글 작성 화면

#### 탭 2: 함뜨해요

**배너 캐러셀** — 5개 추천 모임 (그라데이션 배경, 자동 슬라이드)

**모임 유형 필터** (6개 버튼)
- 온라인 함뜨 / 오프라인 함뜨 / 평일 함뜨 / 주말 함뜨 / 소규모 함뜨 / 대규모 함뜨

**추천 모임 섹션**
- 썸네일 + 온/오프라인 태그
- 모임명, 일정, 인원 (현재/최대)

**요일별 모임 섹션**
- 날짜 탭 (예: 27·28·29·30·31·1·2·3)
- 모임 카드: 시간 + 모임명 + 장소/플랫폼 + 인원 + 참여/마감 버튼

**모임 만들기** 배너 CTA

---

### 4.7 게시글 상세 (`#comm-post-detail`)

- 뒤로가기 버튼 (← 이전 화면 복귀)
- 카테고리 배지 + 작성자 정보
- 제목 + 본문 (리치 텍스트, 이미지 포함 가능)
- 투표 블록 (게시글 내 인라인 투표, 선택지 바 표시)
- 댓글 목록 + 댓글 입력창
- 좋아요 / 댓글 / 공유 액션

---

### 4.8 게시글 작성 (`#comm-write`)

| 요소 | 설명 |
|------|------|
| 카테고리 선택 | 토글 버튼 → 드롭다운 (뜨개 질문 / 뜨개 잡담 / 자투리 마켓 / 구인·외주) |
| 질문글 배지 | "뜨개 질문" 선택 시 노출, 체크 토글 (활성: `#F72E00`) |
| 제목 입력 | 최대 60자 |
| 본문 에디터 | contenteditable, 텍스트 + 사진 + 인라인 투표 블록 |
| 완료 버튼 | 카테고리·제목·내용 모두 입력 시 활성화 (`#F72E00`) |

---

### 4.9 커뮤니티 검색 (`#comm-search`)

**검색바**
- 좌측 뒤로가기 `<` 버튼
- 검색어 입력창 (placeholder: "검색어를 입력해주세요", 커서 숨김 기본)
- 우측 X 버튼 (입력값 삭제, 결과 패널 유지)

**최근 검색 패널** (검색 전)

| 탭 | 내용 |
|----|------|
| 커뮤니티 | 최근 검색어 pill + 추천 키워드(2줄) + 인기 검색어(10위) |
| 함뜨해요 | 최근 검색어 pill + 추천 키워드(2줄) + 인기 검색어(10위) |

**검색 결과 패널** (검색 후)

필터바:
- 정렬: 최신순 / 인기순 (드롭다운)
- 카테고리: 전체 / 뜨개 질문 / 뜨개 잡담 / 자투리 마켓 / 구인·외주

결과 카드 — 게시글 상세 카드와 동일한 레이아웃

**검색 결과 없음 화면**
1. 🔎 이모지
2. **검색어**에 대한 검색 결과가 없어요
3. 다른 검색어를 입력해보세요 (회색)
4. 구분선
5. 추천 키워드 섹션 (2줄)
6. 구분선
7. 인기 검색어 섹션 (2열 10위)

**상태 유지 동작**
- 결과 패널 표시 중 검색창 재활성화 → 결과 유지
- X 버튼 → 입력값만 삭제, 결과 패널 유지
- 게시글 상세 → 뒤로가기 → 결과 패널 복원

---

### 4.10 탐색 (`#explore`)

**검색바**
- 돋보기 아이콘 + 텍스트 입력 ("검색")
- 입력 시 X 클리어 버튼 노출
- 활성화 시 "취소" 버튼 노출 (`#F72E00`)

**검색 패널** (포커스 시 오버레이)

| 탭 | 내용 |
|----|------|
| 스토어 | 최근 검색어 pill + 추천 키워드 + 인기 검색어(10위) |
| 커뮤니티 | 최근 검색어 pill + 추천 키워드 + 인기 검색어(10위) |

- 최근 검색어 우측 "저장 기능 끄기" / "모두 삭제" 버튼
- 최근 검색어 없을 때: "최근 검색어가 없어요" 빈 상태
- 추천 키워드: `#FFF0EE` 배경 pill, 클릭 시 해당 검색어로 검색
- 인기 검색어: 2열 그리드, 순위 `#F72E00`

**검색 결과 패널**

| 탭 | 내용 |
|----|------|
| 게시글 | 커뮤니티 게시글 카드 목록 |
| 계정 | 사용자 프로필 목록 |

**탐색 그리드** (기본 화면)
- 마소너리(Masonry) 3열 그리드
- 일반 셀과 세로로 2배 높은 tall 셀 혼합 배치
- 각 셀: 그라데이션 배경 + 이모지 썸네일
- 오버레이: 하트 아이콘 + 좋아요 수
- 복수 사진 셀: 우상단 멀티 아이콘 표시
- 셀 태그(data-tag): 가디건·모자·숄·양말·아미구루미·코바늘·담요 등

---

### 4.11 마이 프로필 (`#profile`)

- 헤더: 닉네임 (중앙), + 버튼 (새 프로젝트), 메뉴 버튼 (설정)
- 아바타 서클 (이니셜)
- 프로필 카드: 이름, 통계(작품 수·팔로워·팔로잉), 자기소개
- 탭: 게시글 / 태그됨
- 공유된 프로젝트 이미지 그리드

---

### 4.12 프로필 편집 (`#edit-profile`)

| 필드 | 제약 |
|------|------|
| 닉네임 | 최대 20자 |
| 아이디 | 최대 30자, @username 형식 |
| 한줄 소개 | 최대 80자 |
| 프로필 사진 | 이미지 업로드 |

---

### 4.13 설정 (`#settings`)

**계정**
- 프로필 편집
- 활동 내역
- 차단 목록

**앱**
- 앱 정보

**관리자 전용** (숨김, 버전 탭 N회로 활성화)
- 실 데이터 관리

**계정 관리**
- 로그아웃
- 회원 탈퇴

---

### 4.14 챌린지 (`#challenge`)

- 히어로 배너: 이달의 챌린지 (제목 + 기간 + 참여자 수 + 참여 버튼 + 이모지)
- 필터: 전체 / 진행 중 / 예정 / 완료
- 챌린지 카드 목록:
  - 상태 배지 (진행 중=주황, 예정=파랑, 완료=회색)
  - 제목 + 설명
  - 기간 + 참여자 수
  - 달성률 진행 바
  - 리워드: 배지 + 쿠폰
  - 참여하기 / 사전 신청 / 마감됨 버튼

---

### 4.15 알림 (`#notification`)

**헤더**
- 뒤로가기 버튼 (진입 전 화면으로 복귀)
- "알림" 타이틀
- 알림 설정 톱니바퀴 아이콘 → `#notification-settings`

**알림 목록**

| 섹션 | 내용 |
|------|------|
| 새 알림 | 미읽음 알림 + "모두 읽음" 버튼 |
| 이전 알림 | 읽음 처리된 알림 |

**알림 아이템**
- 아바타 (이모지) + 본문 텍스트 + 시간
- 미읽음: 우측 빨간 점 표시
- 케밥 메뉴(⋮): 삭제 / 읽음 처리 옵션

**알림 유형**
- `댓글`: "XXX님이 회원님의 게시글에 댓글을 남겼어요."
- `대댓글`: "XXX님이 회원님의 댓글에 대댓글을 달았어요."
- `좋아요`: "XXX님이 회원님의 게시글에 좋아요를 눌렀어요."

---

### 4.16 알림 설정 (`#notification-settings`)

뒤로가기 → `#notification`

| 섹션 | 항목 | 기본값 |
|------|------|--------|
| **전체** | 알림 허용 (마스터 토글) | ON |
| **커뮤니티** | 댓글 알림 | ON |
| | 대댓글 알림 | ON |
| | 좋아요 알림 | OFF |
| | 함뜨해요 알림 | ON |
| **쇼핑** | 배송 알림 | ON |
| | 재입고 알림 | ON |
| | 할인/이벤트 알림 | OFF |
| **매거진 & 챌린지** | 새 매거진 알림 | OFF |
| | 챌린지 알림 | ON |

- 마스터 토글 OFF → 하위 토글 전체 비활성화

---

### 4.17 채팅 목록 (`#chat`)

**헤더** — "채팅" + 뒤로가기

**검색창** — "대화 검색" placeholder

**채팅방 목록 아이템**
- 아바타 (그라데이션 원형 + 이모지)
- 대화 상대 닉네임 + 마지막 메시지 시간
- 마지막 메시지 미리보기
- 미읽음 수 배지 (숫자, `#F72E00` 배경)

---

### 4.18 채팅 방 (`#chat-room`)

**헤더**
- 뒤로가기 → `#chat`
- 상대방 닉네임 + "니터스 회원" 서브타이틀
- 케밥 메뉴 (⋮)

**메시지 목록**
- 날짜 구분선 ("오늘", "어제" 등)
- 상대방 메시지: 좌측 버블 (아바타 + 회색 버블 + 시간)
- 내 메시지: 우측 버블 (`#F72E00` 배경 + 시간)
- 파일 공유 버블: 파일명 강조 스타일 (PDF 등)

**입력창** (하단 고정)
- 첨부 버튼 (좌)
- 텍스트 입력 ("메시지를 입력하세요")
- 전송 버튼 (우)

---

### 4.19 공지사항 (`#notice`)

- 헤더: "공지사항" + 뒤로가기 → `#community`
- 스크롤 본문 영역 (동적 렌더링)
- 커뮤니티 고정 공지 클릭 시 진입

---

### 4.20 매거진 (`#magazine-list`, `#magazine-article`)

**목록 화면**
- 캐러셀 배너 (5개, 점 인디케이터)
- 카테고리 탭: 전체 / 트렌드 / 인터뷰 / 브랜드 / 팁
- 아티클 카드: 썸네일 + 태그 + NEW 배지 + 제목 + 발행일 + 읽기 시간

**아티클 화면**
- 히어로 섹션 (그라데이션 + 제목 + 이모지)
- 작성자 + 발행일 + 읽기 시간
- 본문 (리치 텍스트)
- 관련 매거진 추천 섹션

---

## 5. 데이터 모델

### 5.1 사용자
```
User {
  id: string
  name: string          // 닉네임 (max 20자)
  handle: string        // @아이디 (max 30자)
  email: string
  bio: string           // 한줄소개 (max 80자)
  avatar: string        // 이미지 URL 또는 이니셜
  isAdmin: boolean
  stats: {
    projectCount: number
    followers: number
    following: number
  }
}
```

### 5.2 프로젝트
```
Project {
  id: string
  name: string
  status: 'none' | 'in_progress' | 'paused' | 'done'
  startDate: string | null   // ISO 8601
  endDate: string | null
  content: string            // HTML (리치 에디터)
  timerMs: number            // 누적 타이머 (밀리초)
  counters: [
    { label: string, value: number },
    { label: string, value: number }
  ]
  isShared: boolean
  createdAt: string
  updatedAt: string
  isUserPost: boolean
}
```

### 5.3 커뮤니티 게시글
```
CommPost {
  id: number
  user: string               // 닉네임
  avatar: string             // 이모지 또는 이미지
  ac: 'blue' | 'light'       // 아바타 배경 색상
  cat: '뜨개 질문' | '뜨개 잡담' | '자투리 마켓' | '구인/외주'
  catCls: string             // CSS 클래스
  title: string
  body: string               // 평문 미리보기
  bodyHtml: string           // HTML 본문 (상세)
  likes: number
  views: string              // "1.2k" 형식
  time: string               // "2시간 전" 형식
  editedAt: string | null
  initComments: Comment[]
  isPinned: boolean
  isUserPost: boolean
}
```

### 5.4 함뜨해요 모임
```
HamtteMeeting {
  name: string
  type: '온라인' | '오프라인'
  time: string               // "매주 토 14:00"
  members: string            // "7/10명"
}
```

### 5.5 댓글
```
Comment {
  u: string          // 작성자 닉네임
  t: string          // 댓글 본문
  replies?: Comment[] // 대댓글
}
```

### 5.6 투표 블록 (게시글 내 인라인)
```
Poll {
  title: string            // 투표 제목 (max 50자)
  options: [
    { text: string, votes: number },
    { text: string, votes: number }
  ]
  totalVotes: number
  userVoted: number | null  // 선택한 옵션 인덱스
}
```

### 5.7 챌린지
```
Challenge {
  id: string
  title: string
  description: string
  emoji: string
  status: 'upcoming' | 'in_progress' | 'completed'
  startDate: string          // ISO 8601
  endDate: string
  participantCount: number
  completionRate: number     // 0–100
  reward: string             // "🎁 배지 + 쿠폰 증정"
}
```

### 5.8 매거진 아티클
```
MagazineArticle {
  id: string
  title: string
  category: '트렌드' | '인터뷰' | '브랜드' | '팁'
  emoji: string
  heroBg: string             // CSS gradient
  author: string             // "니터즈 매거진"
  publishDate: string        // ISO 8601
  readTimeMin: number
  bodyHtml: string
  tags: string[]
  relatedIds: string[]
  isNew: boolean
}
```

### 5.10 로컬 스토리지 키 (계정별 네임스페이스)
| 키 | 내용 |
|----|------|
| `knitters_profile` | 프로필 정보 |
| `knitters_projects_{userId}` | 프로젝트 목록 |
| `knitters_comm_posts` | 사용자 작성 게시글 |
| `knitters_comm_likes` | 좋아요 상태 |
| `knitters_comm_comments` | 댓글 데이터 |

---

## 6. UI 컴포넌트 & 패턴

### 6.1 상태 배지
| 상태 | 한국어 | 색상 |
|------|--------|------|
| `none` | 시작 안 함 | `#9CA3AF` |
| `in_progress` | 진행 중 | `#3B86FB` |
| `paused` | 쉬는 중 | `#F59E0B` |
| `done` | 완성 | `#22C55E` |

### 6.2 바텀 탭 바
5개 탭 (홈 / 기록 / 커뮤니티 / 탐색 / 마이), 활성 탭 아이콘 `#F72E00`

### 6.3 카드
- 모서리: `border-radius: 14px`
- 그림자: `0 4px 16px rgba(0,0,0,0.10), 0 1px 4px rgba(0,0,0,0.06)`
- 배경: `#FFFFFF`

### 6.4 화면 전환 애니메이션
모든 화면 진입 시 동일한 애니메이션 적용:
```css
@keyframes homeIn {
  from { opacity: 0; transform: translateY(18px) scale(0.98); }
  to   { opacity: 1; transform: translateY(0) scale(1); }
}
/* duration: 0.75s, easing: cubic-bezier(.16,1,.3,1), fill: forwards */
```

### 6.5 FAB (Floating Action Button)
- 위치: 우하단, 바텀 탭 위 (`bottom: 94px, right: 20px`)
- 스크롤 다운 시 텍스트 축소 → 아이콘만 표시

### 6.6 시트 / 모달
| 시트 | 트리거 |
|------|--------|
| 상태 선택 | 상태 배지 탭 |
| 날짜 달력 | 날짜 배지 탭 |
| 정렬 드롭다운 | 정렬 버튼 탭 |
| 카테고리 드롭다운 | 글쓰기 카테고리 버튼 탭 |

### 6.7 검색 공통 패턴
- 최근 검색어: pill 형태 (`background: #F3F4F6`)
- 추천 키워드: pill 형태 (`background: #FFF0EE`, 텍스트 `#F72E00`), 2줄 제한
- 인기 검색어: 2열 그리드, 순위 숫자 `#F72E00`

### 6.8 타이포그래피
| 용도 | 크기 | 굵기 |
|------|------|------|
| 대제목 | 20px | 700 |
| 섹션 제목 | 16px | 700 |
| 카드 제목 | 15px | 600 |
| 본문 | 14px | 400 |
| 캡션 / 메타 | 12px | 400–500 |
| 카테고리 배지 | 11px | 500 |

---

## 7. 주요 인터랙션

### 7.1 프로젝트 사진 드래그 재정렬
- 사진 전체를 길게 누르면 드래그 시작 (포인터 이벤트)
- 드래그 중 고스트 이미지 + 빨간 드롭라인 (`#F72E00`) 표시
- 브라우저 기본 드래그 동작 차단 (`draggable="false"`, `setPointerCapture`)

### 7.2 타이머
- 시작 / 일시정지 / 초기화
- 백그라운드에서도 경과 시간 계산 (시작 timestamp 기준)
- 프로젝트 상세·기록 화면 공유

### 7.3 단수 카운터
- 카운터 1·2 각각 독립 동작
- 레이블 편집 (탭 → 입력)
- `+` / `−` / 리셋

### 7.4 커뮤니티 검색 상태 유지
- 결과 패널 표시 중 재포커스 → 결과 유지
- X 버튼 → 쿼리 초기화, 결과는 유지
- 뒤로가기 → `_goingBack` 플래그로 상태 복원

### 7.5 스트릭 시스템
- 활동 일자 기록 → 연속 일수 계산
- 요일별 스탬프 시각화

### 7.6 인라인 투표 (Poll)
- 게시글 작성 시 툴바 투표 버튼으로 에디터 내 투표 블록 삽입
- 투표 제목 (max 50자) + 선택지 2개 텍스트 입력
- 게시글당 투표 블록 1개 제한
- 게시글 상세에서 선택지 탭 → 투표 참여, 퍼센트 바 즉시 업데이트
- 이미 투표한 선택지는 파란 배경으로 하이라이트

### 7.7 FAB 스크롤 축소
- 기록 / 커뮤니티 페이지의 FAB: 스크롤 40px 이상 시 텍스트 숨기고 아이콘만 표시
- `transition: padding 0.25s, max-width 0.25s` 애니메이션

### 7.8 캐러셀
- 매거진·함뜨해요 배너: 터치 스와이프 또는 자동 슬라이드
- 점(dot) 인디케이터로 현재 위치 표시
- 무한 루프 없음 (첫/마지막 슬라이드에서 멈춤)

---

## 8. 알림 시스템

| 유형 | 설명 |
|------|------|
| 챌린지 알림 | 참여 중인 챌린지 업데이트 |
| 커뮤니티 알림 | 내 게시글 좋아요·댓글 |
| 모임 알림 | 참여 중인 함뜨 모임 리마인더 |

- 홈 헤더 알림 아이콘에 미읽음 빨간 점 표시
- 알림 설정 화면에서 유형별 ON/OFF

---

## 9. 비기능 요구사항

| 항목 | 내용 |
|------|------|
| 플랫폼 | 모바일 브라우저 (iOS Safari, Android Chrome) |
| 구현 방식 | 단일 HTML 파일 SPA (Vanilla JS) |
| 데이터 저장 | localStorage (계정별 네임스페이스) |
| 배포 | GitHub Pages (main 브랜치 자동 배포) |
| 반응형 | 모바일 최적화 (max-width 430px 기준) |
| 언어 | 한국어 전용 |
